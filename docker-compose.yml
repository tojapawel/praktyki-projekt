version: '3'

services:


  db:
    image: mysql:5.7
    volumes:
      - ./db-server/db:/var/lib/mysql
      - ./db-server/DB_DUMP:/var/www/DB
    environment:
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "33075:3306"