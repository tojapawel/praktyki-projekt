version: '3'

services:
  # api-server:
  #   build:
  #     context: ./api-server
  #   ports:
  #     - 3001:3001
  #   command: npm start

  # hotel-finder:
  #   build:
  #     context: ./hotel-finder
  #   ports:
  #     - 3000:3000
  #   command: npm run dev

  db:
    image: mysql:5.7
    volumes:
      - ./db-server/db:/var/lib/mysql
      - ./db-server/DB_DUMP:/var/www/DB
    environment:
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "33075:3306"